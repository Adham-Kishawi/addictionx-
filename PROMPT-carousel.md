# مهمة: كاروسيل المنتجات بالأدوار (Role-based Carousel) — ADDICTIONX

> اقرأ `CLAUDE.md` أولًا. كل قواعد المشروع سارية.
> **لا تلمس أي شيء خارج نطاق هذه المهمة.** لا الأدمن، لا الكتالوج، لا صفحة المنتج،
> لا الـ hero، ولا أي قسم آخر في الصفحة الرئيسية.

---

## الهدف

**إضافة** قسم كاروسيل سينمائي جديد في `src/app/[lang]/page.tsx`، يوضع **قبل** قسم
«الأكثر طلبًا» مباشرة. تتبادل فيه المنتجات الأدوار: واحد في المنتصف كبيرًا وحادًا،
واثنان على الجانبين صغيران ومموّهان.

**قسم «الأكثر طلبًا» يبقى كما هو تمامًا — لا تحذفه ولا تعدّله.**

### الترتيب الجديد للصفحة

```
Hero → Marquee → StatsBand → 🆕 الكاروسيل → الأكثر طلبًا → المجموعات → ...
```

### ماذا يعرض الكاروسيل؟ (قرار مهم)

**منتجًا واحدًا من كل مجموعة** — لا الـ bestsellers.

**السبب:** لو عرض الكاروسيل الـ bestsellers، فالشبكة تحته تعرض **نفس المنتجات مرتين
متتاليتين** — يبدو كخلل لا كتصميم. اختيار منتج عن كل مجموعة يجعل الكاروسيل
«واجهة المجموعات»، ويطابق تمامًا الصور المتوفرة (RUSH · NOIR · GOLD).

في `page.tsx`، `collections` و `allProducts` متوفران بالفعل. اختر أول منتج من كل مجموعة،
وفضّل المنتج **الذي يملك `image`** إن وُجد أكثر من واحد.

---

## الفكرة الجوهرية: الدور، لا المكان

هذا هو مربط الفرس. **لا تحرّك أي عنصر من مكان إلى مكان.**

كل عنصر يسأل سؤالًا واحدًا: «ما دوري الآن؟» — والجواب يُحسب من رقم واحد فقط:

```ts
const center = activeIndex;
const right = (activeIndex + 1) % total;
const left = (activeIndex + total - 1) % total;
const back = total > 3 ? (activeIndex + 2) % total : null;
```

ولكل دور **ستايل ثابت محفوظ مسبقًا**. عند تغيّر `activeIndex` تتبادل الأدوار تلقائيًا،
و CSS transition هو من يرى الفرق بين الستايل القديم والجديد وينفّذ الحركة.

**ملاحظة رياضية مهمة:** استخدم `(activeIndex + total - 1) % total` وليس
`(activeIndex - 1) % total`. في JavaScript الـ modulo يأخذ إشارة البسط، فـ
`-1 % 3` تساوي `-1` لا `2` — ستحصل على index سالب والعنصر يختفي.

### ⚠️ دور `back` يُلغى عند 3 عناصر أو أقل

المرجع مبني على 4 عناصر. مع **3** عناصر يقع تعارض:

```
back = (i + 2) % 3
left = (i + 2) % 3   ← نفس العنصر!
```

عنصر واحد سيحاول أن يكون في مكانين، فيهتزّ أو يختفي.

**الحل:** مع 3 عناصر أو أقل، اقتصر على `center` و `left` و `right` فقط (`back = null`).
اكتب المنطق ليعمل مع أي عدد ≥ 3 — لا تُثبّت الرقم 3 ولا 4 في الكود، فقد تُضاف
مجموعة رابعة لاحقًا.

**لا تستخدم Framer Motion هنا.** CSS transitions أخفّ وأدق لهذه الحالة، ولا حاجة
لمكتبة. هذا استثناء مقصود من قاعدة «Framer Motion فقط» وسببه الأداء — سجّله في `CLAUDE.md`.

---

## ⚠️ الاختلافات الإجبارية عن المرجع

### 1. الخلفية سوداء دائمًا — التوهج فقط هو ما يتغيّر

المرجع يغيّر لون خلفية الشاشة بالكامل مع كل عنصر (برتقالي/أخضر/بمبي/أزرق).
هذا **يدمّر هوية ADDICTIONX** (أسود عميق + نيون أحمر + فضي).

**البديل:** خلفية القسم تبقى كما هي (`bg-background`)، ويتغيّر **لون التوهج**
عبر `radial-gradient` خلف المنتج المركزي، بلون `product.art.glow` للعنصر النشط.

```
background: radial-gradient(60% 50% at 50% 55%, ${glow}26, transparent 70%)
transition: background 650ms cubic-bezier(0.4, 0, 0.2, 1)
```

الحقل `art.glow` موجود بالفعل في نوع `Product`.

### 2. النص العملاق — لاتيني دائمًا وبـ opacity منخفض

المرجع: `28vw` أبيض بـ `opacity: 1`. على خلفية سوداء سيكون صارخًا ويطمس المنتجات.

- المحتوى: `ADDICTIONX` — **دائمًا لاتيني**، مع `dir="ltr"` صريح.
- **السبب:** `whiteSpace: nowrap` مع نص عربي بحجم `28vw` سيخرج عن الشاشة —
  الكلمات العربية أطول والاتجاه RTL. لا تقرأ هذا النص من القاموس.
- الخط: `var(--font-playfair)` (موجود) — لا تُضف Anton ولا أي خط جديد.
- `opacity: 0.05` مع `color: white`، أو `-webkit-text-stroke: 1px rgb(255 255 255 / 0.08)`
  مع `color: transparent` (الثاني أفخم).
- `aria-hidden` + `select-none` + `pointer-events-none`.

### 3. لا حاجة لطبقة الـ Grain

المرجع يضيف SVG noise overlay. المشروع لديه هويته البصرية بالفعل — تجاهلها.

---

## البنية

مكوّن client جديد: `src/features/catalog/components/product-carousel.tsx`

```tsx
type ProductCarouselProps = {
  products: Product[]; // 3 عناصر حاليًا — اكتب المنطق لأي عدد ≥ 3
  locale: Locale;
  wishlistIds: string[] | null; // للتمرير إلى WishlistButton
};
```

**حماية إجبارية:** لو `products.length < 3` لا تعرض الكاروسيل إطلاقًا (`return null`)
— منطق الأدوار يحتاج ثلاثة عناصر على الأقل. قاعدة البيانات قد تكون فارغة أو ناقصة.

يُستدعى من `page.tsx` (Server Component) ببيانات serializable فقط.
**لا تمرّر دوال ولا أيقونات** — خطأ مثبَّت في المشروع.

### ستايلات الأدوار

| الدور  | scale | blur | opacity | z   | left | height |
| ------ | ----- | ---- | ------- | --- | ---- | ------ |
| center | 1.0   | 0    | 1       | 20  | 50%  | 100%   |
| left   | 0.58  | 2px  | 0.8     | 10  | 22%  | 62%    |
| right  | 0.58  | 2px  | 0.8     | 10  | 78%  | 62%    |
| back   | 0.5   | 4px  | 0.55    | 5   | 50%  | 54%    |

> دور `back` مستخدم فقط عند 4 عناصر أو أكثر. مع 3 عناصر يُهمل تمامًا.

- كل عنصر: `position: absolute`, `aspectRatio: "0.6 / 1"`, `transform: translateX(-50%) scale(...)`
- `bottom: 0` للجميع — الاتساق في خط القاعدة هو ما يجعل المشهد يبدو صحيحًا.
- `transition: transform 650ms cubic-bezier(0.4,0,0.2,1), filter 650ms …, opacity 650ms …, left 650ms …`
- `willChange: "transform, filter, opacity"` — **أضفها فقط أثناء الحركة وأزلها بعدها**
  (تركها دائمًا يستهلك ذاكرة GPU بلا داعٍ).

### قفل الحركة

```ts
if (isAnimating) return;
setIsAnimating(true);
// ... تغيير activeIndex
setTimeout(() => setIsAnimating(false), 650);
```

**السبب:** الحركة تأخذ 650ms. نقرتان سريعتان تزيدان `activeIndex` مرتين والـ transition
في منتصفه، فيتنافس عنصران على نفس الدور وتظهر قفزة بصرية. الرقم يجب أن يطابق
مدة الـ transition بالضبط. نظّف الـ timeout في `useEffect` cleanup.

---

## الصور

الصور الثلاثة PNG بخلفية شفافة (نسبة `0.6/1`) مرفوعة على Cloudinary.

- استخدم `next/image` مع `fill` + `objectFit: contain` + `objectPosition: bottom center`.
- `sizes` مضبوطة (لا تحمّل صورة كاملة العرض لعنصر بـ scale 0.58).
- `priority` على العنصر المركزي فقط، والباقي `loading="lazy"`.
- `draggable={false}` على كل صورة.
- **fallback إجباري:** لو `product.image` غير موجود، اعرض `ProductArt` بنفس الأبعاد.
  عدد المنتجات في DB أكبر من عدد الصور المتوفرة — لا تفترض وجود صورة لكل منتج.

`res.cloudinary.com` مسموح بالفعل في `next.config.ts` — لا تعديل مطلوب.

---

## ما كان ناقصًا في المرجع — أضفه

هذه ليست تحسينات اختيارية. بدونها المكوّن غير مقبول في مشروع production.

### السحب باللمس (swipe)

المرجع يعتمد على زرّين فقط. على الجوال يتوقع المستخدم السحب بإصبعه — بدونه التجربة ناقصة.

- `onPointerDown` / `onPointerMove` / `onPointerUp` (Pointer Events تغطي اللمس والماوس معًا).
- عتبة: 50px أفقيًا.
- **تجاهل السحب الرأسي:** لو `Math.abs(deltaY) > Math.abs(deltaX)` فالمستخدم يمرّر
  الصفحة لا يتنقّل — لا تعترض الحركة ولا تستدعِ `preventDefault`.
- `touchAction: "pan-y"` على الحاوية.

### الكيبورد

- الحاوية `tabIndex={0}` مع `role="region"` و `aria-roledescription="carousel"`.
- `ArrowLeft` / `ArrowRight` تتنقّل. **احترم RTL:** في العربية السهم الأيمن يعني
  «السابق» بصريًا — اعكس المنطق حسب `locale`.
- `focus-visible` ظاهر بوضوح على الحاوية والأزرار.

### `prefers-reduced-motion`

استخدم `useReducedMotion` من framer-motion (مستخدم بالفعل في المشروع).
عند التفعيل: `transition: none` مع بقاء التنقّل يعمل، و`filter: none` (الـ blur نفسه مزعج).

### قارئ الشاشة

- منطقة `aria-live="polite"` مخفية بصريًا تُعلن اسم المنتج النشط ورقمه: «٢ من ٤».
- الأزرار لها `aria-label` من القاموس (السابق/التالي) — لا نصوص مكتوبة في الكود.
- العناصر غير المركزية `aria-hidden` — لا تُربك التنقّل بالكيبورد.

### الإيقاف عند الخروج من الشاشة

`IntersectionObserver`: لو القسم خارج الشاشة، لا تشغّل أي حركة.

---

## المحتوى حول الكاروسيل

- اسم المنتج النشط + سعره (`formatPrice` من `products.ts` — **بالقروش، لا تكتب حسابًا يدويًا**).
- زر «اكتشف» يقود إلى `/{locale}/product/{slug}` للعنصر النشط.
- `WishlistButton` للعنصر النشط فقط (مرّر `wishlisted` من `wishlistIds`).
- النقر على عنصر جانبي **ينقله للمنتصف** لا يفتح صفحته — النقر على المركزي فقط يفتح الصفحة.
- الأزرار: `ArrowLeft` / `ArrowRight` من `lucide-react`، دائرية بحدود، والمشروع RTL
  فتأكد من اتجاه الأيقونات (`rtl:rotate-180` مستخدم بالفعل في المشروع).
- كل النصوص من `dict.*`. أضف المفاتيح الناقصة **في AR و EN معًا**.
- لفّ القسم بـ `Reveal` الموجود (`@/components/motion/reveal`) للحفاظ على الاتساق.
- استخدم `SectionHeading` الموجود بعنوان جديد يخصّ المجموعات (لا تكرّر عنوان
  «الأكثر طلبًا» ولا عنوان قسم «المجموعات» الموجود أسفل الصفحة).
- **تمييز بصري عن القسم الذي يليه:** الكاروسيل والشبكة سيصبحان متجاورين، فأضف
  فاصلًا واضحًا — مسافة رأسية كبيرة، أو حدّ خفيف، أو خلفية `bg-card/40` للكاروسيل
  (مستخدمة بالفعل في أقسام أخرى بالصفحة). بدون ذلك سيبدو القسمان ملتصقين.

---

## التحقق قبل التسليم

```bash
npm run type-check
npm run lint
npm run build
```

اختبار حيّ على 3100:

0. **قسم «الأكثر طلبًا» لا يزال موجودًا كما كان** — لم يُحذف ولم يتغيّر.
   والكاروسيل يعرض منتجات مختلفة عنه (لا تكرار مرئي).
1. التنقّل سلس بلا قفزات، والنقر السريع المتكرر لا يكسر شيئًا (القفل يعمل).
   1b. مع 3 عناصر: ثلاثة مواضع فقط ولا عنصر يهتزّ أو يختفي (دور `back` مُلغى).
2. لون التوهج يتغيّر مع المنتج، والخلفية تبقى سوداء.
3. النص العملاق لا يخرج عن الشاشة في `/ar` ولا `/en`.
4. swipe يعمل على الجوال، والتمرير الرأسي **لا يزال يعمل** داخل القسم.
5. الأسهم تعمل بالكيبورد بالاتجاه الصحيح في RTL و LTR.
6. `prefers-reduced-motion`: تنقّل فوري بلا حركة ولا blur.
7. المنتجات بلا صورة تعرض `ProductArt` بلا كسر في التصميم.
8. السعر مطابق لما يظهر في الكتالوج (تحقق من صحة `formatPrice`).

---

## أخطاء مثبَّتة — تجنّبها

- `params` async — `await params` إجباري (Next 16).
- **لا تُمرَّر دوال أو أيقونات من Server إلى Client** — بيانات serializable فقط.
- `dict.*` من النوع `as const` — أي prop يقبل مصفوفة منه اجعله `readonly string[]`.
- Base UI Button يستخدم `render={<Link/>}` — **لا** `asChild`.
- الأسعار `Int` بالقروش — استخدم `formatPrice` حصرًا.

---

## في النهاية

حدّث `CLAUDE.md`: مكوّن `product-carousel` وفكرة الأدوار · استثناء CSS transitions
من قاعدة Framer Motion وسببه · قرار الخلفية السوداء الثابتة مع التوهج المتغيّر ·
قرار النص العملاق اللاتيني.
