# مهمة: دمج Hero سينمائي تفاعلي بالفيديو في ADDICTIONX

> اقرأ `CLAUDE.md` أولًا. كل قواعد المشروع فيه سارية (Framer Motion فقط، transform/opacity،
> `prefers-reduced-motion` إجباري، Server Components افتراضيًا).
> **لا تلمس أي شيء خارج نطاق هذه المهمة.** لا تعديل على الأدمن، ولا على الكتالوج، ولا على الأقسام
> الموجودة في الصفحة الرئيسية بعد الـ hero.

---

## الهدف

استبدال محتوى الـ hero الحالي في `src/app/[lang]/page.tsx` بمشهد **فيديو تفاعلي يُسحَب بالماوس**:
المنتج يلف يمينًا وشمالًا حسب موضع مؤشر الماوس أفقيًا على الشاشة.

**المبدأ الجوهري:** الفيديو **لا يُشغَّل** (`play()` غير مستخدم على الديسكتوب).
موضع الماوس الأفقي يُترجَم إلى رقم، والرقم يُكتب مباشرة في `video.currentTime` كل frame.

```
زمن الفيديو = زاوية المنتج. والماوس هو من يحدد الزمن.
```

---

## الأصول (موجودة بالفعل)

```
public/videos/bottle-left.mp4     ← من الأمام ← إلى الشمال
public/videos/bottle-right.mp4    ← من الأمام ← إلى اليمين
```

الملفان يبدآن **بنفس الـ frame الأمامي حرفيًا** (وُلِّدا من فيديو واحد مقصوص في المنتصف).
لا تفترض غير ذلك ولا تضف منطق مزامنة بينهما.

---

## قبل أي كود: أمر واحد

```bash
npm uninstall gsap && npm install gsap
```

**السبب:** GSAP حاليًا في `devDependencies`. عند البناء على Vercel لا تُثبَّت الـ devDeps،
فأي استيراد له سيفشل في الإنتاج. هذه المهمة لا تحتاج مكتبات جديدة — Framer Motion و GSAP موجودان.

---

## ⚠️ ثلاث مشاكل قائمة في الكود يجب إصلاحها أولًا

هذه ليست اقتراحات تحسين. بدونها المشهد **لن يعمل**.

### المشكلة 1 (قاتلة): `PageTransition` يكسر كل `position: fixed`

`src/components/motion/page-transition.tsx` يلفّ كل الصفحات في `motion.div` مع `y: 14 → 0`.

الـ `transform` على عنصر أب **يُنشئ containing block جديدًا**، فيتحول أي `position: fixed`
داخله إلى ما يشبه `absolute` بالنسبة لذلك العنصر. المشهد بالكامل قائم على `fixed`
(الفيديو، الـ overlay) — **الفيديو لن يثبت في مكانه، بل سيتحرك مع تمرير الصفحة.**

**الإصلاح:** اجعل `PageTransition` يستخدم **`opacity` فقط** بدون `y` وبدون `will-change: transform`
وبدون `filter`.

```
initial={{ opacity: 0 }}  →  animate={{ opacity: 1 }}
```

`opacity` يُنشئ stacking context لكنه **لا يُنشئ containing block** للـ fixed — فهو آمن.
الانتقال بين الصفحات سيبقى ناعمًا، فقط بدون الإزاحة الرأسية.

> لا تحل هذا باستثناء الصفحة الرئيسية من `PageTransition` — الإصلاح العام أنظف وأقل هشاشة.

### المشكلة 2: الـ Header يطفو فوق المشهد

`src/components/layout/header.tsx` عليه `fixed z-30` مع `backdrop-blur`.
سيظل شريطًا ضبابيًا فوق المشهد السينمائي ويكسر الغمر (immersion).

**الإصلاح:** الهيدر يبدأ **شفافًا تمامًا بلا حدود ولا blur** فوق الـ hero، ويستعيد
خلفيته الضبابية بعد تمرير أول 80% من ارتفاع الشاشة.

طريقة التنفيذ: مكوّن client صغير يستمع لـ `scroll` (بـ `passive: true`) ويبدّل الـ classes.
لا تحوّل `Header` بالكامل إلى client component — أضف wrapper أو مكوّن حالة صغير فقط.
نصوص الهيدر تبقى بيضاء فوق الـ hero مع `drop-shadow` خفيف للوضوح.

### المشكلة 3: `CursorGlow` يتنافس على نفس المؤشر

`src/components/motion/cursor-glow.tsx` على `z-[90]` ويتبع المؤشر بـ spring.
الفيديو أيضًا سيتبع نفس المؤشر — توهجان على نفس اليد = تشويش، والمستخدم لن يفهم
أن حركته هي التي تلف المنتج.

**الإصلاح:** خفّض `opacity` الـ CursorGlow إلى ما يقارب `0.25` داخل الـ hero فقط،
ويعود لطبيعته بعد الخروج منه. الأبسط: نفس مستمع الـ scroll في المشكلة 2 يضبط
CSS variable على `<html>` (مثل `--cursor-glow-opacity`) يقرأها الـ CursorGlow.

---

## الوضع النهاري (قرار مثبَّت)

الفيديو void أسود بتوهج نيون — **لا يمكنه التكيّف مع خلفية فاتحة**، وسيبدو كمستطيل
أسود مقصوص وسط صفحة بيضاء.

**القرار:** قسم الـ hero **يبقى أسود دائمًا، مستقلًا عن الثيم**.

- خلفية الـ hero: أسود ثابت (`#0a0a0a` أو `oklch(0.12 …)` من الهوية) — **ليس** `bg-background`.
- كل نصوص الـ hero: أبيض ثابت — **ليس** `text-foreground`.
- باقي أقسام الصفحة تتبع الثيم كما هي الآن، بلا تغيير.

يُقرأ هذا القسم ذهنيًا كـ«مشهد/فيديو» لا كـ«خلفية صفحة»، فداكنته مقبولة تمامًا في
الوضع النهاري (نفس منطق مشغّل فيديو داكن داخل صفحة فاتحة). والانتقال من الأسود إلى
الفاتح بعد الـ hero يصبح مقصودًا ودراميًا.

**سجّل هذا القرار في `CLAUDE.md`.**

---

## بنية المشهد

### ترتيب الطبقات (من الأسفل للأعلى)

| z-index | العنصر                                                |
| ------- | ----------------------------------------------------- |
| 0       | حاوية الفيديو (`fixed inset-0`)                       |
| 5       | تدرّج أسود شفاف للتباين                               |
| 8       | `ParticleField` (نسخة مخفّفة)                         |
| 20      | اللوجو + العنوان + النصوص + `HeartbeatLine` + الأزرار |
| 30      | الهيدر (شفاف فوق الـ hero)                            |
| 90      | `CursorGlow` (مخفّف داخل الـ hero)                    |

### طبقة التدرّج (z-5)

`linear-gradient` أسود شفاف من الأعلى والأسفل نحو المنتصف — يضمن تباين النصوص
دون أي blend mode. هذه هي الطبقة التي تحل مشكلة القراءة، لا الـ blend modes.

### اللوجو والنصوص — ظل، لا blend mode

**ممنوع تمامًا استخدام `mix-blend-mode: exclusion` على أي عنصر أحمر أو على اللوجو.**

السبب: `exclusion` **يقلب الألوان** — النيون الأحمر (`oklch(0.6 0.22 22)`) سيتحول إلى
سماوي/تركوازي فوق التوهج الأحمر، فتنكسر هوية العلامة بصريًا.

**البديل:** ظل ناعم بطبقتين — واحدة ضيقة للحدّة وأخرى واسعة للعمق:

```
drop-shadow(0 1px 2px rgb(0 0 0 / 0.6)) drop-shadow(0 4px 24px rgb(0 0 0 / 0.45))
```

يُطبَّق على اللوجو والعنوان والنصوص وأيقونات الهيدر فوق الـ hero.
اللون لا يُلمَس — الأحمر يبقى أحمر.

### `ParticleField` — نسخة مخفّفة

عدّل `src/components/motion/particle-field.tsx` ليقبل props اختيارية مع قيم افتراضية
تساوي سلوكه الحالي (كي لا يتأثر أي استخدام قائم):

- عدد الجسيمات: من 24 إلى **10** في الـ hero.
- `mix-blend-mode: screen` — **ليس** `exclusion`. `screen` يضيف نورًا دون قلب الألوان،
  فالأحمر يبقى أحمر.
- `opacity` أخفّ (حوالي 0.3 من الحالي).

الهدف أن تُقرأ كـ«غبار في شعاع الضوء» لا كـ«نويز» فوق فيديو حقيقي.

---

## منطق التفاعل مع الفيديو

مكوّن client جديد: `src/components/motion/hero-video-scrub.tsx`

### الديسكتوب (`pointer: fine` وعرض ≥ 1024px)

1. الفيديوان `muted` + `playsInline` + `preload="auto"`، مموضعان `absolute inset-0`
   بـ `object-fit: cover`. **لا `autoPlay`.**
2. الأيسر `display: none` والأيمن `display: block` ابتداءً.
3. حلقة `requestAnimationFrame` واحدة تقرأ آخر `clientX` مسجَّل وتحدّث `currentTime`.

**منطقة الحياد (dead zone):**

```
deadZone = Math.max(30, window.innerWidth * 0.05)
```

- المؤشر داخل منطقة الحياد → أبقِ الفيديو النشط على `currentTime = 0` (الوضع الأمامي).
- المؤشر يسار منطقة الحياد → أظهر فيديو **الشمال**، واحسب التقدّم من حدّ المنطقة إلى حافة الشاشة اليسرى.
- المؤشر يمين منطقة الحياد → أظهر فيديو **اليمين**، بنفس المنطق نحو اليمين.
- `activeSideRef` يتغيّر **فقط** عند تجاوز منطقة الحياد.

التقدّم: `(المسافة من حدّ المنطقة) / (المدى المتاح)` مقيسًا على `0 … video.duration`،
مع `clamp` بين 0 و 1.

**شرط حرجّ لمنع الاهتزاز:**

```
if (!video.seeking) video.currentTime = target;
```

بدون هذا الشرط تُطلَب عملية seek جديدة قبل انتهاء السابقة، فيصبح التشغيل متقطعًا.
انتظر حتى ينتهي المتصفح من رسم الإطار السابق.

### تسلسل زمني — لا تزامن

`AnimatedTitle` يطبّق `blur(14px)` على كل حرف، والـ blur **مكلف جدًا على الـ GPU**.
تشغيله بالتوازي مع سحب الفيديو (الذي يطلب seek كل frame) = تنافس على الـ GPU وتهنيج في الاثنين.

**الحل:** الفيديو يبقى ساكنًا على الـ frame الأمامي حتى تنتهي حركة دخول العنوان
(حوالي **1000ms** — الحركة الحالية 0.9s + تأخير 0.25s)، ثم تبدأ حلقة الـ RAF بالاستماع للماوس.

هذا أفضل تجربةً أيضًا: يرى المستخدم العلامة أولًا، ثم يكتشف أنه يتحكم.

### الموبايل واللمس (< 1024px أو `pointer: coarse`)

لا يوجد ماوس، فلا سحب. **تشغيل تلقائي متبادل:**

- الأيسر يعمل أولًا؛ عند `ended` انتقل للأيمن؛ عند انتهائه ارجع للأيسر.
- `muted` + `playsInline` إجباريان (وإلا يمنع iOS التشغيل).
- أضف تلميحًا بصريًا خفيفًا بأن اللف تلقائي هنا — لا توهم المستخدم بتحكم غير موجود.

### `prefers-reduced-motion`

أظهر **إطارًا ثابتًا واحدًا** (الوضع الأمامي) بلا أي حركة، ولا تشغّل حلقة RAF إطلاقًا.
لا تكتفِ بإيقاف الحركة — لا تبدأ الحلقة من الأساس.

### التحميل والأداء

- ابدأ بـ `opacity: 0` على حاوية الفيديو، وانتقل إلى `1` (`transition: opacity 0.3s`)
  عند تحمّل **كلا** الفيديوين (`loadeddata`).
- `poster` بإطار أمامي ثابت لتجنّب فراغ أسود في أول رسم.
- أوقف حلقة الـ RAF تمامًا عند `scrollY > window.innerHeight` (لا معنى لتحديث فيديو غير مرئي)،
  وأضف `visibility: hidden` على الحاوية عندها.
- أوقف الحلقة عند `document.hidden` وأعِد تشغيلها عند العودة.
- نظّف كل المستمعات و `cancelAnimationFrame` في دالة التنظيف الخاصة بـ `useEffect`.

---

## ما يبقى في الـ hero وما يُحذف

**يُحذف:** `AuroraBackground` من الصفحة الرئيسية فقط (لا تحذف الملف — قد يُستخدم لاحقًا).
السبب: خلفية حمراء متحركة فوق فيديو فيه توهج أحمر حقيقي = تنافس على نفس المساحة اللونية،
والتوهج الحقيقي داخل الزجاج يضيع في توهج مزيّف فوقه.

**يبقى:** `AnimatedTitle` · `HeartbeatLine` · الشارة (badge) · النص الفرعي · الزرّان ·
مؤشر التمرير · `ParticleField` بالنسخة المخفّفة.

**بلا أي تغيير:** كل الأقسام بعد الـ hero (Marquee · StatsBand · الأكثر طلبًا · المجموعات ·
شريط التجربة · CTA) وكل قراءات الـ DB الموجودة في الصفحة.

---

## الوصول (a11y)

- حاوية الفيديو `aria-hidden` + `pointer-events-none` — عنصر تزييني بحت.
- العنوان يبقى `<h1>` قابلًا للقراءة (`aria-label` موجود بالفعل في `AnimatedTitle`).
- التفاعل بالماوس **إضافة تجميلية**: كل المحتوى والروابط تعمل بالكيبورد تمامًا بدونه.
- تباين النصوص فوق التدرّج ≥ 4.5:1.

---

## التحقق قبل التسليم

```bash
npm run type-check
npm run lint
npm run build
```

ثم اختبار حيّ على المنفذ 3100:

1. الفيديو **ثابت** عند التمرير (يثبت أن إصلاح `PageTransition` نجح) — أهم اختبار.
2. حركة الماوس يمينًا/شمالًا تلف المنتج بسلاسة بلا اهتزاز ولا قفزات.
3. المرور بمنتصف الشاشة **لا يُظهر أي قفزة** بين الفيديوين.
4. النيون الأحمر يبقى **أحمر** في كل الحالات (لا سماوي في أي لحظة).
5. الهيدر شفاف فوق الـ hero ويستعيد خلفيته بعد التمرير.
6. الوضع النهاري: الـ hero يبقى أسود، وباقي الصفحة يفتح.
7. الموبايل: تشغيل تلقائي متبادل بلا أخطاء console.
8. `prefers-reduced-motion` مفعّلًا: إطار ثابت، ولا حلقة RAF (تحقق من الـ Performance tab).
9. RTL و LTR سليمان (`/ar` و `/en`).

---

## أخطاء مثبَّتة في المشروع — تجنّبها

- `params` و `searchParams` **async** — `await params` إجباري (Next 16).
- **لا تُمرَّر دوال أو أيقونات من Server إلى Client Component** — نصوص وبيانات serializable فقط.
- `dict.*` من النوع `as const` — أي prop يقبل مصفوفة من القاموس اجعله `readonly string[]`.
- Base UI Button يستخدم `render={<Link/>}` — **لا** `asChild`.
- مكوّن Server يستورد `motion` مباشرة يخرّب الـ SSR — `"use client"` إجباري.

---

## في النهاية

حدّث `CLAUDE.md`: قرار الـ hero الأسود دائمًا · إصلاح `PageTransition` وسببه ·
سلوك الهيدر الشفاف · مكوّن `hero-video-scrub` · موقع أصول الفيديو.
