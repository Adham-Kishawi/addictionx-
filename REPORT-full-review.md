# تقرير مراجعة شاملة — ADDICTIONX

> **إلى:** المودل الذي يعمل على الكود.
> **من:** مراجعة قراءة-فقط للمستودع بتاريخ 2026-08-07 (لم يُعدَّل أي ملف).
> **الأولوية:** الفيديو/الهيرو أولًا، ثم التكامل، ثم التجربة (عميل + أدمن)، ثم الثيم واللغة والحركة.

اقرأ القسم صفر قبل أي شيء — فيه خطأ يُسقط المتصفح، وخطأ يُسقط صفحة الأدمن.

---

## 0. أخطاء حاجزة (Blocking) — رتّبها بهذا الترتيب

### 0.1 🔴 الـ sprite يفجّر ذاكرة المتصفح (الأخطر في المشروع كله)

في `hero-video-scrub.tsx` اللوحان يُرسمان بـ:

```tsx
backgroundSize: `${COLS * 100}% ${ROWS * 100}%`; // = 1000% 600%
```

على عنصر `absolute inset-0` — أي بمقاس الشاشة كاملة.

معنى `1000% 600%` أن المتصفح مطالب برسم الصورة بأبعاد `عرض_الشاشة × 10` في `ارتفاع_الشاشة × 6`. الحساب الفعلي:

| الشاشة    | حجم الرسم المطلوب | البكسلات  | الذاكرة للوح الواحد | ولوحين       |
| --------- | ----------------- | --------- | ------------------- | ------------ |
| 1440×900  | 14400×5400        | 78 مليون  | ~311 MB             | **~622 MB**  |
| 1920×1080 | 19200×6480        | 124 مليون | ~498 MB             | **~996 MB**  |
| 2560×1440 | 25600×8640        | 221 مليون | ~885 MB             | **~1.77 GB** |

الملف على القرص 400KB — لكن الـ decoded bitmap في الذاكرة هو ما يهم، و`background-size` بالنسب يُجبر إعادة رسم بهذا الحجم. النتيجة على أجهزة كثيرة: تبطيؤ حاد، أو تجاهل الـ GPU للـ texture فيسقط للـ software rasterization (وهذا بالضبط سبب أي "بطء" تراه)، أو تعطّل التاب على 4K.

وأسوأ: **اللوحان يُرسمان على الموبايل أيضًا.** لا `display:none` ولا `hidden lg:block` عليهما — فيديو الموبايل يغطيهما بصريًا فقط (لأنه بعدهما في الـ DOM)، لكن الموبايل يدفع تكلفة الرسم كاملة بلا أي مقابل.

**الحل:** لا تُمدِّد الشبكة على الشاشة. اجعل كل لوح بحجم **إطار واحد** ثم كبّره بـ `transform: scale`، أو — الأنظف والمعتمد صناعيًا — استخدم `<canvas>` وارسم الإطار بـ `drawImage(sprite, sx, sy, 640, 360, 0, 0, w, h)`:

- الذاكرة تصبح حجم الـ sprite الأصلي مرة واحدة (6400×2160 ≈ 55 MB) + canvas بمقاس الشاشة، بدلًا من نصف جيجابايت لكل لوح.
- الرسم فوري كما هو (لا seek).
- تحصل مجانًا على تحكّم `object-fit: cover` صحيح (انظر 0.2).
- لوح واحد بدل اثنين: `drawImage` يختار الـ sprite المناسب فلا حاجة لتبديل `display`.

وفي كل الأحوال: أضف `hidden lg:block` على اللوحين، و`lg:hidden` موجود بالفعل على حاوية الفيديو — حتى لا يدفع الموبايل ثمن الديسكتوب.

### 0.2 🔴 نسبة الإطار مشوّهة على أي شاشة ليست 16:9

الخلية في الشبكة `640×360` = **16:9**. اللوح `inset-0` = نسبة الشاشة. مع `background-size` بالنسب المئوية، الخلية تُمطّ لتطابق نسبة الشاشة — فالزجاجة تُشد أفقيًا على 16:10 (1440×900، وهو أكثر مقاس لابتوب شائع) وعلى 3:2 و21:9.

مع `<canvas>` تحلّها بحساب cover يدويًا. مع `background` لا يوجد حل نظيف — وهذا سبب إضافي للانتقال إلى canvas.

### 0.3 🔴 `TOTAL = 60` بينما `right.mp4` فيه 59 إطارًا فقط

قياس فعلي بـ `ffprobe`:

```
left.mp4  : 1280×720 · 24fps · nb_frames=60 · duration=2.499
right.mp4 : 1280×720 · 24fps · nb_frames=59 · duration=2.457
```

الكود يستخدم `TOTAL = COLS * ROWS = 60` لكلا الجانبين. فعندما يبلغ المؤشر أقصى اليمين، الفهرس 59 على `right.jpg` = **خلية فارغة سوداء** → الشاشة تنقلب أسود عند حافة الشاشة. و`CLAUDE.md` يوثّق `FRAMES = 59` — أي أن التوثيق يصف إصلاحًا **غير موجود في الكود**.

**الحل:** عدد إطارات مستقل لكل جانب.

```ts
const FRAMES = { left: 60, right: 59 } as const;
```

واستخدم `FRAMES[side] - 1` في كل `clamp` و`round` وفي الـ bob. الأفضل طويل الأمد: أعد توليد الفيديوين بعدد إطارات متطابق (`-frames:v 60`) وثبّت الرقم، لأن أي إعادة توليد بأرقام مختلفة ستكسر هذا مرة أخرى بصمت.

### 0.4 🔴 `deleteProduct` يُسقط صفحة الأدمن بخطأ Prisma خام

`src/features/admin/actions.ts:277`

```ts
export async function deleteProduct(id: string) {
  await requireAdmin();
  await prisma.product.delete({ where: { id } });
  revalidatePath("/", "layout");
}
```

في `schema.prisma:293` علاقة `OrderItem.product` **بلا `onDelete`** — أي `Restrict` افتراضيًا (وهذا صحيح ومقصود: لا يجوز أن يمحو حذف منتج تاريخ الطلبات). لكن لا يوجد `try/catch`، فأي محاولة لحذف منتج **سبق بيعه مرة واحدة** ترمي `P2003` غير ملتقط → شاشة خطأ Next، بلا رسالة مفهومة، والأدمن لا يعرف السبب.

**الحل:** التقط الحالة وأرجع كودًا مثل باقي الـ actions:

```ts
const orderedCount = await prisma.orderItem.count({ where: { productId: id } });
if (orderedCount > 0) return { error: "HAS_ORDERS" }; // اقترح إخفاءه بـ isActive
```

وأضف مفتاحي قاموس (`productHasOrders` + توجيه للمستخدم أن يستخدم زر الإخفاء). لاحظ أن `toggleProductActive` موجود بالفعل — فالمسار البديل جاهز، ينقص فقط التوجيه.

---

## 1. ما هو منجَز فعلًا (تحقق على القرص)

`CLAUDE.md` دقيق في هذا الجزء — كل ما يوثّقه موجود. 100 ملف في `src`:

**الستورفرنت:** الرئيسية (هيرو sprite + marquee + StatsBand + كاروسيل + الأكثر طلبًا + المجموعات + التجربة + CTA) · الكتالوج بفلاتر وترتيب · صفحة المنتج بهرم النوتات والتقييمات · السلة (Zustand + drawer) · الـ checkout بالكوبونات · تسجيل/دخول + Google provider.

**حساب العميل:** تبويبات (نظرة عامة/طلبات/عناوين/مفضلة) · تفاصيل طلب + تتبع شحنة · CRUD عناوين · إلغاء طلب.

**الأدمن:** 15 صفحة — منتجات (CRUD + بحث + فلتر sale) · طلبات (+ طلب يدوي) · مستخدمون · كوبونات · مجموعات · تقييمات · نشرة · إعدادات شحن.

**البنية:** Auth.js v5 + أدوار بحماية مزدوجة · Prisma مع `onDelete` مضبوط في 13 علاقة · أسعار `Int` بالقروش · i18n بـ 911 سطر قاموس AR/EN · ثيم CSS variables · `src/lib/email.ts` · رفع صور.

**الأصول:** `left.mp4` `right.mp4` `sprites/{left,right}.jpg` `slider/{rush,noir,gold}.png` (1024×1536 PNG).

**جودة:** صفر `any` · صفر `@ts-ignore` · طبقة البيانات مفصولة (`products-db.ts`).

**وإصلاحان من الجولة السابقة مؤكَّدان:** `gsap` انتقل إلى `dependencies` ✅ · `PageTransition` صار opacity-only مع تعليق يشرح السبب ✅.

---

## 2. الفيديو / الهيرو — بقية الملاحظات

### 2.1 🟠 `cancelBob` لا يُلغي شيئًا فعلًا

```ts
const cancelBob = useCallback(() => {
  bobActiveRef.current = false;
  gsap.killTweensOf(mouseXRef); // ← لا يقتل أي شيء
}, []);
```

الـ bob يُحرّك كائنًا محليًا `const target = { frame: 0 }` داخل `runBob` — و`mouseXRef` لم يُحرَّك بـ gsap أبدًا. فالنتيجة أن الـ tween **يظل حيًّا** بعد `cancelBob`، وكل `onUpdate` فيه يكتب `backgroundPosition`، في نفس اللحظة التي عاد فيها `frame()` للكتابة (لأن `bobActiveRef` صار `false`). كاتبان يتنازعان على نفس الخاصية كل إطار = رجفة عند أول حركة ماوس بعد الخمول.

**الحل:** ارفع الكائن إلى `useRef` واقتله بـ `gsap.killTweensOf(bobTargetRef.current)`. أو أبسط: تخلَّ عن gsap هنا واكتب الـ bob بـ `requestAnimationFrame` مع easing يدوي — سطور قليلة، وتُسقط تبعية 70KB من الـ hero وتُلغي هذه الفئة من الأخطاء بالكامل.

### 2.2 🟡 الموبايل يحمّل 800KB ولا يستخدمها

`ready` معلّق على `img.decode()` للشبكتين — على الموبايل الذي لا يستخدمهما مطلقًا. الحاوية كلها `opacity: 0` حتى ينتهي فك ترميز صورتين لا لزوم لهما. **الحل:** حدّد `isMobile` مرة في `useState` initializer وتخطَّ التحميل تمامًا؛ على الموبايل اجعل `ready` معلّقًا على `loadeddata` للفيديو.

### 2.3 🟡 `translateX(-5px)` الموثّق غير موجود

`CLAUDE.md` (بند 18) يقول إن اللوح الأيمن عليه `transform: translateX(-5px)` لمطابقة كادرات الجانبين. لا وجود له في الكود. إما أن القفزة عند تبديل الجانبين ما زالت قائمة، أو أن الحاجة انتهت والتوثيق قديم. **افحص بصريًا** (مرّر المؤشر عبر مركز الشاشة ببطء) واحسم أحد الأمرين: إما أضف الإزاحة، وإما احذف السطر من التوثيق.

### 2.4 🟡 مفتاحا قاموس ميّتان

`dict.hero.autoRotate` و`dict.hero.dragToRotate` معرّفان AR+EN ولا يُستدعيان في أي `.tsx`. مخلّفات قرار "لا تلميح". احذفهما من القاموس أو احذف السطر الذي يذكرهما من `CLAUDE.md`.

### 2.5 🟢 ما هو صحيح في الهيرو (لا تلمسه)

الهيرو أسود ثابت `#0a0a0a` مستقل عن الثيم · التدرّج الأسود للتباين بلا `mix-blend` · `ParticleField count={10} blend="screen"` (الأحمر يبقى أحمر) · النصوص بظل بطبقتين لا `exclusion` · طبقات z مرتّبة (0/5/8/20/30/90) · `HeaderScroll` يشفّف الهيدر ويخفت `CursorGlow` إلى 0.25 عبر متغير CSS · إيقاف الـ RAF عند `document.hidden` وعند تجاوز التمرير · خروج fade + `visibility:hidden` · `prefers-reduced-motion` يقطع كل شيء قبل أي RAF · `aria-hidden` + `pointer-events-none`.

**قرار مهم في المعمار:** لا تعُد إلى `video.currentTime` seeking. الانتقال إلى الإطارات المستخرجة صحيح ونهائي. المشكلة في **كيف** تُرسم الإطارات (0.1 و0.2)، لا في الأسلوب.

---

## 3. الكاروسيل

### 3.1 🟠 `roleOf` يُعيد `"center"` كسقوط افتراضي

```ts
if (back !== null && i === back) return "back";
return "center"; // ← أي عنصر بلا دور يصير مركزيًا
```

بثلاث مجموعات هذا غير مرئي. لحظة أن يضيف walid **مجموعة رابعة من الداشبورد** (وهي ميزة مبنية وجاهزة)، يظهر عنصر خامس بلا دور فيُرسم بحجم كامل فوق المركز. صفحة مكسورة بلا أي تعديل كود.

**الحل:** أضف الدور `hidden` (`opacity: 0`, `scale: 0.4`, `zIndex: 0`, `pointerEvents: "none"`) واجعله السقوط الافتراضي.

### 3.2 🟡 `height` في `ROLE_STYLES` غير مستخدم

كل الأدوار تعرّف `height` (100/62/62/54) ولا يُقرأ أبدًا — العرض ثابت `62%` والحجم يأتي من `scale` وحده. إما طبّقه أو احذفه؛ الحقل الميت يوهم المشرف القادم أن هناك تحكّمًا في الارتفاع.

### 3.3 🟢 صحيح: تصحيح `back` عند 3 عناصر · قفل `isAnimating` مطابق لـ 650ms · swipe يمرّر السحب الرأسي · كيبورد RTL-aware · `IntersectionObserver` · `aria-live` · النص العملاق لاتيني `dir="ltr"` · `formatPrice`.

---

## 4. تكامل الموقع — النواقص التي تفصله عن "متكامل"

هذا القسم هو الفرق بين متجر جاهز للإطلاق ومتجر شبه جاهز. مرتّب بالأثر.

### 4.1 🔴 لا قائمة تنقّل على الموبايل

`header.tsx` — الـ `<nav>` عليها `hidden ... md:flex`، ولا يوجد أي زر هامبرغر ولا drawer بديل. **على الموبايل لا يستطيع المستخدم الوصول إلى المتجر أو المجموعات إطلاقًا** إلا بالضغط على اللوجو والتمرير. في متجر مصري حيث أغلب الترافيك موبايل، هذه ليست ملاحظة تصميم — هذه خسارة مبيعات مباشرة.

أضف زر Menu + drawer (نفس نمط `cart-drawer` الموجود) يضم: الرئيسية · المتجر · المجموعات (قائمة من `getCollections()`) · حسابي · الأدمن إن كان أدمن.

### 4.2 🔴 لا بحث في الستورفرنت

`name="q"` موجود في `admin/products` فقط. لا يستطيع الزائر البحث عن عطر بالاسم. في متجر عطور بالتحديد، البحث بالاسم هو نية الشراء الأقوى. الـ `getProducts()` جاهز والفلترة في `catalog/page.tsx` تعمل على مصفوفة — إضافة `q` سطران، وحقل بحث في الهيدر يقود إلى `/catalog?q=...`.

### 4.3 🔴 لا صفحات خطأ ولا تحميل ولا 404

مفقود بالكامل: `not-found.tsx` · `error.tsx` · `global-error.tsx` · `loading.tsx` (أي مستوى).

كل الصفحات `force-dynamic` تقرأ من DB، فأي منتج محذوف أو رابط خاطئ أو انقطاع DB = شاشة Next الافتراضية الرمادية بالإنجليزية، تكسر الهوية واللغة والثيم فورًا. و`notFound()` مُستدعى فعلًا في `product/[slug]` — أي أن هناك مسارًا مضمونًا لصفحة 404 لا وجود لها.

المطلوب: `[lang]/not-found.tsx` · `[lang]/error.tsx` (client، بزر إعادة محاولة) · `app/global-error.tsx` · و`loading.tsx` للكتالوج وصفحة المنتج والأدمن على الأقل (skeleton بألوان العلامة — فرصة إبهار لا عبء).

### 4.4 🔴 SEO شبه غائب

`generateMetadata` موجودة في `[lang]/layout.tsx` **وحدها**. أي أن كل صفحات المتجر تتقاسم عنوانًا واحدًا. تحديدًا:

- **`product/[slug]` بلا `generateMetadata`** — لا عنوان، لا وصف، لا OG image، لا `canonical`، لا JSON-LD `Product`/`Offer`/`AggregateRating`. صفحة المنتج هي صفحة الهبوط الأولى من البحث ومن مشاركات واتساب، وحاليًا تُشارَك بلا صورة ولا سعر.
- لا `sitemap.ts` ولا `robots.ts`.
- لا `hreflang` بين `/ar` و`/en` — وهذا كان أحد أسباب اختيار `[lang]` segment أصلًا، فالفائدة غير محصّلة.
- لا `openGraph.images` في أي مكان.

### 4.5 🟠 لا صفحات ثقة

مفقود: من نحن · تواصل معنا · سياسة الاستبدال والإرجاع · سياسة الخصوصية · الشروط · الأسئلة الشائعة. في متجر مصري يبيع عطورًا بالدفع عند الاستلام، صفحة الإرجاع والهاتف/واتساب هي ما يحوّل الزائر المتشكك إلى مشتر. الفوتر حاليًا يربط إلى الرئيسية والكتالوج والدخول فقط.

### 4.6 🟠 لا استرجاع كلمة مرور

لا `forgot-password` ولا `reset-password`. `Resend` مثبّت و`lib/email.ts` جاهز — البنية موجودة، الميزة غائبة. مستخدم Credentials ينسى كلمته يفقد حسابه نهائيًا.

### 4.7 🟠 لا صفحة تأكيد طلب مستقلة

النجاح يُعرض داخل حالة في `checkout-form`. لا مسار `/order/[number]` أو `/checkout/success`. النتيجة: تحديث الصفحة يفقد التأكيد، ولا رابط يُشارَك أو يُرجَع إليه، **ولا يوجد مسار يمكن لإيميل التأكيد أن يقود إليه** (انظر 4.8).

### 4.8 🟠 إيميلات بلا روابط

`grep` على `src/lib/email.ts` لا يجد أي `http` ولا أي مسار طلب. القوالب نصية بلا CTA. المستخدم يستلم "تم تأكيد طلبك" ولا يستطيع النقر للوصول إليه. أضف `NEXT_PUBLIC_APP_URL` واربط كل قالب بـ `/{lang}/account/orders/{id}`.

### 4.9 🟡 لا ترقيم صفحات في أي مكان

`catalog` تجلب كل المنتجات وتفلتر في الذاكرة · `admin/orders` تجلب كل الطلبات · `admin/newsletter` كل المشتركين. بثمانية منتجات لا مشكلة. بمئة منتج وألف طلب تصبح كل زيارة استعلامًا ثقيلًا وصفحة بطيئة. الفلترة والترتيب أيضًا يجب أن تنزل إلى Prisma (`where`/`orderBy`/`take`/`skip`) بدلًا من `Array.filter`/`sort` بعد جلب الكل.

### 4.10 🔴 رفع الصور لن يعمل على Vercel

`api/admin/upload-image/route.ts` يكتب إلى `public/uploads` بـ `writeFile`. نظام ملفات Vercel للقراءة فقط (وعابر في أي حال) — الرفع سيفشل في الإنتاج، والصور المرفوعة محليًا لن تُنشر. الطريق مرسوم بالفعل في `CLAUDE.md` (Cloudinary، `next.config.ts` يسمح بـ `res.cloudinary.com` بالفعل) — الميزة معلّقة على بيانات حساب walid. **يجب أن تُعلَّم كحاجب للنشر، لا كتحسين.**

### 4.11 🟡 لا حد معدّل (rate limiting)

`grep` لا يجد أي rate limiting. مكشوف: التسجيل (إنشاء حسابات آلي) · النشرة (تلقيم بريد الغير) · التقييمات · `validateCoupon` (تخمين أكواد الخصم بالقوة الغاشمة — الأخطر ماليًا). أضف حدًّا بسيطًا بالـ IP على الأقل على التسجيل والنشرة و`validateCoupon`.

### 4.12 🟡 `force-dynamic` على كل شيء يضرب هدف 90+ Lighthouse

عشرون ملفًا فيها `force-dynamic`. للأدمن والحساب هذا صحيح تمامًا. لكن الرئيسية والكتالوج وصفحة المنتج محتوى عام يتغيّر عند تعديل الأدمن فقط — والـ `revalidatePath` مستخدم بالفعل في كل الـ actions. حوّلها إلى ISR/static + `revalidateTag`. المكسب كبير: TTFB و LCP وتكلفة الاستضافة. تنبيه: `getWishlistIds()` و`auth()` في هذه الصفحات هي ما يجبرها على الديناميكية — افصل الأجزاء الشخصية إلى client component يجلب حالة المفضلة بعد التحميل، أو إلى `<Suspense>` مستقل.

---

## 5. الثيم (الوضع الليلي)

🟡 **لا احترام لتفضيل النظام.** سكربت `theme-init` يقرأ `localStorage.theme` فقط. الزائر الجديد الذي جهازه على الوضع الفاتح يُقابَل بالمظلم دائمًا. `viewport.themeColor` يعرّف الحالتين، فالبنية موجودة نصفها. أضف السقوط: `localStorage.theme ?? (matchMedia("(prefers-color-scheme: light)").matches ? "light" : "dark")` — مع بقاء المظلم افتراضيًا عند عدم وجود تفضيل. وأضف مستمع `change` للتحوّل الحي.

🟡 **الوضع الفاتح غير مُختبر بصريًا.** المتغيرات معرّفة في `:root[data-theme="light"]` — لكن الصفحة مليئة بـ `oklch(0.6 0.22 22 / 0.08)` مكتوبة inline في تدرّجات (الرئيسية، الكاروسيل، CTA) وبـ `rgba(255,255,255,0.07)` في النص العملاق و`from-black/70` في بطاقات المجموعات. هذه كلها مضبوطة على خلفية سوداء ولا تتكيّف. راجع الرئيسية والكاروسيل وصفحة المنتج في الوضع الفاتح — التوهجات الحمراء الخفيفة تختفي على الأبيض، والنص العملاق الأبيض الشفاف يصير غير مرئي، وتدرّج `from-black/70` قد يبقى صحيحًا (فوق صورة) أو يصير ثقيلًا.

🟢 صحيح: الهيرو أسود دائمًا مستقلًا عن الثيم (قرار مثبّت وسليم) · الثيم في `localStorage` بلا وميض · `header-over-hero` يعمل في الحالتين.

---

## 6. اللغة والـ RTL

🟢 قوي عمومًا: 911 سطر قاموس متطابق AR/EN · `dir`/`lang` من الـ segment · `proxy.ts` يحوّل `/` وتجاهل الأصول صحيح (بما فيه `mp4`) · `rtl:rotate-180` مطبَّق على أسهم الاتجاه · النص العملاق `dir="ltr"` · اللوجو `dir="ltr"`.

النواقص:

🟠 **لا `hreflang` ولا `alternates.languages`** — نصف قيمة `[lang]` مهدرة (كررتها من 4.4 لأنها تخص اللغة تحديدًا).

🟡 **`LanguageSwitcher` — تحقق من حفظ المسار.** يجب أن يبدّل `/ar/product/x?sort=y` إلى `/en/product/x?sort=y` بنفس المسار والـ query. إن كان يعيد إلى الجذر فهي خسارة سياق مزعجة.

🟡 **`formatPrice` يستعمل `"en-EG"` دائمًا** — أي أرقامًا لاتينية في الواجهة العربية. هذا **قد يكون مقصودًا** (الأرقام اللاتينية أكثر شيوعًا في التجارة المصرية) — إن كان كذلك فوثّقه في `CLAUDE.md` كقرار حتى لا "يصلحه" أحد لاحقًا فيكسر الاتساق.

🟡 **الأخطاء التقنية غير مترجمة.** أكواد مثل `NOT_CANCELLABLE` و`STOCK` و`HAS_ORDERS` تحتاج مفتاح قاموس لكل واحد. تحقق أن كل كود خطأ تُرجعه الـ actions له نص عربي/إنجليزي مقابل — وإلا يرى العميل كودًا خامًا.

---

## 7. الحركة (Animation)

🔴 **لا سقوط CSS لـ `prefers-reduced-motion`.** `grep "reduced"` على `globals.css` = صفر نتائج. الحماية كلها على مستوى JS بـ `useReducedMotion`، لكن الحركات المكتوبة كـ CSS keyframes **تتجاهلها تمامًا**: `metallic-shine` (5s لا نهائي على كل عنوان) · `marquee-x` · `heartbeat-pulse` (على زر CTA، لا نهائي) · `animate-bounce`. مستخدم حسّاس للحركة (دوار، صرع ضوئي، ADHD) سيرى شريطًا يتحرك بلا توقف وزرًا ينبض. أضف الكتلة:

```css
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

🟡 **`ParticleField`: 24 عنصرًا × `boxShadow` = 24 طبقة blur دائمة.** `box-shadow` بنصف قطر 12px على عنصر متحرك يُعاد تركيبه كل إطار. في الهيرو مخفّف إلى 10 (جيد)، لكن أي استخدام آخر بالافتراضي يظل ثقيلًا. البديل: `radial-gradient` كخلفية بدل `boxShadow` — نفس المظهر بتكلفة أقل بكثير.

🟡 **`AnimatedTitle`: `filter: blur(14px)` على كل حرف.** مع عنوان 12 حرفًا = 12 طبقة blur متزامنة على الـ GPU. الترتيب الزمني (~1000ms قبل تشغيل الـ RAF) يعالج التنازع مع الهيرو — لكن على أجهزة متوسطة الـ blur وحده قد يُسقط إطارات في أول ثانية، وهي أول انطباع للزائر. اختبره على throttle 4x؛ إن تعثّر خفّض إلى `blur(8px)` أو استبدله بـ `scaleY` — الأثر البصري يبقى.

🟢 صحيح: `PageTransition` opacity-only مع تعليق يشرح السبب (لا تُرجعه أبدًا إلى `y`) · `Reveal` بـ `whileInView` مرة واحدة · `CursorGlow` مقيّد بـ `pointer: fine` ويخفت فوق الهيرو · استثناء CSS transitions للكاروسيل موثّق ومبرَّر.

---

## 8. تجربة الأدمن

🟢 قوي: 15 صفحة · حماية مزدوجة (layout + `requireAdmin()` في كل action) · بحث وفلتر sale في المنتجات · طلب يدوي للمكالمات · شحن يُنشأ تلقائيًا عند SHIPPED · إعدادات الشحن من DB · إعادة المخزون عند الإلغاء في المسارين (عميل وأدمن) · منع تغيير دور المرء لنفسه ومنع حذف نفسه · تنبيه مخزون منخفض.

النواقص:

🔴 **`deleteProduct`** — قسم 0.4.

🟠 **لا تأكيد قبل العمليات المدمّرة.** تحقق من وجود `confirm()` أو dialog في `delete-user-button` و`collection-delete` و`coupon-actions` و`product-actions`. حذف مستخدم يفصل طلباته نهائيًا (`userId → null`) وهو غير قابل للتراجع — يجب أن يمرّ بتأكيد صريح يذكر العدد المتأثر.

🟠 **لا ترقيم في الطلبات والنشرة** — قسم 4.9. صفحة الطلبات ستصبح غير عملية عند أول مئة طلب.

🟡 **لا تصدير CSV.** الطلبات (لشركة الشحن) والنشرة (لمنصة الإيميل) والمنتجات (للجرد) — كلها ستُطلب عمليًا في الأسبوع الأول من التشغيل الحقيقي.

🟡 **لا سجل تدقيق (audit log).** مع تعدد الأدمن لن تعرف من غيّر سعرًا أو حذف كوبونًا. جدول `AuditLog` بسيط (actor/action/entity/before/after) يُكتب في نفس معاملة كل action.

🟡 **الداشبورد يعرض إحصاءات بلا نطاق زمني.** لا "اليوم/الأسبوع/الشهر" ولا إيراد إجمالي ولا متوسط قيمة الطلب. أرقام بلا مقارنة لا تُقاد بها قرارات.

🟡 **لا صفحة `admin/reviews` للـ pending وحدها.** الصفحة تعرض الكل — والأدمن يحتاج "ما ينتظر موافقتي" كافتراضي.

---

## 9. الأمن والبيانات

🟢 صحيح ومهم: السعر يُحسب في السيرفر من DB حصرًا (لا ثقة في السلة) · خصم المخزون ذرّي داخل معاملة · تحقق الكوبون النهائي في `createOrder` · ملكية العناوين بشرط `userId` في `updateMany`/`deleteMany` · `requireAdmin()` في كل action لا في الـ UI · `bcryptjs` · رفع الصور محدود بالنوع والحجم ومحمي بالدور · صفر `any` · `onDelete` مضبوط في 13 علاقة.

النواقص:

🔴 كلمة مرور الأدمن في `prisma/seed.ts` — غيّرها قبل النشر (موثّق في `CLAUDE.md`، أبقيه ظاهرًا في قائمة ما قبل الإطلاق).
🟡 لا rate limiting (4.11).
🟡 لا CSRF/origin check على `api/admin/upload-image` بخلاف فحص الدور — مقبول مع session cookie، لكن أضف فحص `Origin` للاحتياط.
🟡 لا `Content-Security-Policy` ولا headers أمنية في `next.config.ts`.
🟡 التحقق من البريد غير مطبَّق (`emailVerified` في الـ schema بلا مسار تفعيل).

---

## 10. الأولويات المقترحة

**الجولة أ — حاجب للنشر:**

1. الـ sprite إلى canvas (0.1 + 0.2) — أثر ذاكرة/أداء أكبر من كل ما تبقى مجتمعًا.
2. `FRAMES` لكل جانب (0.3).
3. `deleteProduct` (0.4).
4. قائمة تنقّل الموبايل (4.1).
5. `not-found` + `error` + `global-error` + `loading` (4.3).
6. `prefers-reduced-motion` في CSS (قسم 7).
7. `cancelBob` (2.1) + إخفاء اللوحين على الموبايل (0.1).

**الجولة ب — تكامل:** 8. `generateMetadata` لصفحة المنتج + JSON-LD + `sitemap` + `robots` + `hreflang` (4.4). 9. بحث الستورفرنت (4.2). 10. صفحة تأكيد الطلب + روابط في الإيميلات (4.7 + 4.8). 11. استرجاع كلمة المرور (4.6). 12. صفحات الثقة (4.5). 13. Cloudinary (4.10) — معلّق على بيانات walid.

**الجولة ج — نضوج:** 14. ترقيم + نزول الفلترة إلى Prisma (4.9). 15. ISR بدل `force-dynamic` للصفحات العامة (4.12). 16. تفضيل ثيم النظام + مراجعة الوضع الفاتح بصريًا (قسم 5). 17. rate limiting (4.11) + headers أمنية. 18. تأكيدات الحذف + CSV + audit log + نطاق زمني للداشبورد (قسم 8). 19. `roleOf` fallback في الكاروسيل (3.1) — قبل إضافة أي مجموعة رابعة. 20. تنظيف: مفاتيح `hero` الميتة · `height` الميت · مطابقة `CLAUDE.md` مع الكود (2.3 + 0.3).

---

## 11. مطلوب من walid (حواجب خارجية)

| البند                                   | الأثر                                                           |
| --------------------------------------- | --------------------------------------------------------------- |
| `RESEND_API_KEY`                        | بدونه كل الإيميلات تُسجَّل ولا تُرسَل — العميل لا يستلم تأكيدًا |
| `AUTH_GOOGLE_ID` + `AUTH_GOOGLE_SECRET` | الزر ظاهر ويفشل عند الضغط — **أخفه حتى تتوفر المفاتيح**         |
| Cloudinary (cloud name + key + secret)  | **حاجب نشر** — رفع الصور لن يعمل على Vercel (4.10)              |
| كلمة مرور أدمن جديدة                    | أمني                                                            |
| `NEXT_PUBLIC_APP_URL`                   | لازم لروابط الإيميلات و`canonical` والـ OG                      |

---

## 12. ملاحظة على `CLAUDE.md`

الملف دقيق وشامل بدرجة عالية — وهو أصل حقيقي للمشروع. انحرافان فقط، وكلاهما يخص الهيرو:

- `FRAMES = 59` موثّق و`TOTAL = 60` في الكود (0.3) — والتوثيق هنا هو **الصحيح**، فالكود هو ما يجب أن يتبع.
- `translateX(-5px)` موثّق وغير موجود (2.3).

بعد أي إصلاح، حدّث البندين 18 و18d. وأضف قرارين جديدين يستحقان التثبيت:

- **قرار canvas:** لماذا `background-position` على `1000% 600%` مرفوض (الحساب في 0.1) — وإلا سيعيده مشرف قادم بحجة "أبسط".
- **قرار `formatPrice` بأرقام لاتينية** في الواجهة العربية إن كان مقصودًا.
